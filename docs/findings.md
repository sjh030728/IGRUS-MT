# findings — 개발 큐

**학습 세션이 발견했지만 고칠 수 없는 것들.** 개발 세션이 가져가서 처리하고 **지운다**
(체크가 아니라 삭제 — 큐지 로그가 아니다).

이 파일은 `CLAUDE.md`가 `@docs/findings.md`로 임포트한다 — 매 세션 시작에 자동 주입되므로
읽으러 갈 필요가 없고, 그래서 짧게 유지한다. 항목 형식은 `.claude/commands/learn.md` 규칙 1.

---

**비어 있음.** 그게 정상이다.
