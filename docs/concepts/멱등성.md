---
tags:
  - 영역/상태-정확성
---

# 멱등성 (idempotency)

> **스텁.** `/learn 멱등성` 으로 물어보면 채워진다. #스텁/성장

**한 줄:** 여러 번 해도 한 번 한 것과 결과가 같은 성질.

**이 프로젝트에서:**
- `packages/protocol/src/events.ts` — `LIVE_COMMIT`에 "matchId에 대해 멱등" 주석.
  사회자가 커밋을 두 번 눌러도 점수가 두 번 안 들어간다

**왜 필요한가:** 8시 25분에 사회자가 마이크를 들고 있고 응답이 0.2초 늦으면 한 번 더 누른다.
반드시 그런다.

**궁금해할 만한 것:** 순수 함수와 뭐가 다른가? `ADJUST`는 왜 멱등이 아닌가?
(힌트: "3조 +10"을 두 번 누르면 뭐가 맞는 동작인가?)

**관련 개념:** [[순수-함수와-결정성]], [[append-only-원장과-fold]]
