---
tags:
  - 영역/계약-타입
---

# 브랜드 타입 (branded type)

> **스텁.** `/learn 브랜드 타입` 으로 물어보면 채워진다. #스텁/성장

**한 줄:** 런타임엔 그냥 문자열인데 타입만 서로 다르게 "도장"을 찍어서 섞어 쓰면 컴파일 에러가
나게 하는 기법.

**이 프로젝트에서:**
- `packages/protocol/src/ids.ts` — `TeamId`, `ParticipantId`, `RoundId` 등 전부

**막으려는 버그:**
```ts
deltas[teamId]        // 맞음
deltas[participantId] // 도장이 없으면 이것도 통과 → 8시 35분에 점수가 조용히 틀림
```

**궁금해할 만한 것:** 런타임 비용이 0인 이유는? `TeamId.parse('t1')`를 매번 해야 하는 게
귀찮지 않나? (힌트: 값을 만드는 지점이 실제로 몇 군데인지 세어볼 것)

**관련 개념:** [[스키마-기반-타입-추론]]
