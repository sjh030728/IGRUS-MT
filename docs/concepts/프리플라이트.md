---
tags:
  - 영역/운영
---
#스텁

# 프리플라이트 (preflight)

**한 줄:** 진짜로 쓰기 전에 미리 한 번 돌려봐서, 실패를 쓰는 순간(8:03)이 아니라
고칠 수 있는 시간(7:30)으로 끌어오는 것. 비행기가 뜨기 전 점검 목록에서 온 말.

어디서 나왔나: `round.service.ts`의 `start()` — 부팅 때 모든 GAME 세그먼트의
`loadRounds()`를 미리 돌려보고, 문제은행이 비었으면 그 자리에서 죽는다.
