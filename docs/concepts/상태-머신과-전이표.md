---
tags:
  - 영역/상태-정확성
---

# 상태 머신과 전이표

> **스텁.** `/learn 상태 머신` 으로 물어보면 채워진다. #스텁

**한 줄:** "지금 어느 단계인가"를 정해진 몇 개 중 하나로 두고, 단계 사이 이동 규칙을 한 곳에
모아두는 것.

**이 프로젝트에서:**
- `packages/protocol/src/phase.ts` — `RoundPhase` 8개 + `LEGAL_TRANSITIONS`. LockReveal의 코어
- `packages/protocol/src/live.ts` — `LivePhase` 5개 + `LEGAL_LIVE_TRANSITIONS`. 별개의 머신

**막으려는 것:** 이게 없으면 "입력받는 중인데 공개가 눌렸다" 같은 상황을 코드 여기저기서
`if`로 막게 되고, 어딘가 하나는 반드시 빠뜨린다.

**궁금해할 만한 것:** 왜 LockReveal과 Live의 머신을 공통 조상으로 안 합쳤나?
`HostSnapshot.legal`을 서버가 파생해서 내려주는 이유는? (힌트: 콘솔이 전이표를 재구현하면
무슨 일이 생기나)

**관련 결정:** [[0001-계약을-먼저-정의하고-원칙을-타입에-박는다]]
**관련 개념:** [[판별-유니온]]
