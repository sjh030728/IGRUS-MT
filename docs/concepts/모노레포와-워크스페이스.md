# 모노레포와 워크스페이스

> **스텁.** `/learn 모노레포` 로 물어보면 채워진다. #스텁/성장

**한 줄:** 여러 패키지를 한 레포에 두고 서로 import하게 하는 구조. npm/pnpm의 워크스페이스가
그걸 지원한다.

**이 프로젝트에서:**
- `package.json`(루트) — `workspaces: ["packages/*", "apps/*"]`
- `packages/protocol/package.json` — `@mt/protocol`. 서버와 빔/폰이 이걸 import한다

**왜 이렇게 했나:** 계약이 한 벌이어야 한다. 복사하면 어긋난다. [[0001-계약을-코드보다-먼저-정의한다]]

**궁금해할 만한 것:** `main`이 `.ts`를 가리키는데 왜 되나(보통 빌드된 `.js`를 가리킨다)?
`tsconfig.json`의 `types: []`가 왜 중요한가? (힌트: 이 패키지를 브라우저도 import한다)
pnpm 대신 npm으로 간 이유는? → [[0001-계약을-코드보다-먼저-정의한다]]

**관련 개념:** [[스키마-기반-타입-추론]]
