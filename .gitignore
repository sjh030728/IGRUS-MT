node_modules/
dist/
*.tsbuildinfo

# 옵시디언이 보관함(docs/)에 만드는 설정 폴더.
# workspace.json이 창을 옮길 때마다 바뀌어서 커밋 노이즈가 된다. 1인이라 설정 동기화 불필요.
.obsidian/
.env
.env.local
.DS_Store

# ★실전 퀴즈 시드 — 실명 개인정보라 커밋 금지 (docs/survey.md)★
# 예시(quiz.questions.example.json)는 가짜 이름이라 커밋해도 된다.
apps/server/db/quiz.questions.json
